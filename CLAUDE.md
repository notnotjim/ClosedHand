<!--
Project rules live in AGENTS.md, so every coding agent reads the same file rather
than each vendor keeping its own copy. Claude Code only auto-loads CLAUDE.md, so
this imports it. Nothing else belongs in here: add project rules to AGENTS.md and
personal preferences to ~/.claude/CLAUDE.md, which is never committed.

This comment is stripped before the file reaches Claude's context, so it explains
itself to a human without costing tokens.
-->
@AGENTS.md
